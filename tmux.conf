# change prefix to C-f
set-option -g prefix C-f
unbind-key C-b
bind-key f send-prefix
bind-key C-w send-prefix

# vim keys in copy and choose mode
set-window-option -g mode-keys vi

# copying selection vim style
# http://jasonwryan.com/blog/2011/06/07/copy-and-paste-in-tmux/
# https://github.com/myfreeweb/dotfiles/blob/master/tmux.conf
bind-key Escape copy-mode			# enter copy mode; default [
bind-key p paste-buffer				# paste; (default hotkey: ] )
bind-key P choose-buffer 			# tmux clipboard history
bind-key + delete-buffer \; display-message "Deleted current Tmux Clipboard History"

bind-key -T copy-mode-vi v send-keys -X begin-selection;
bind-key -T copy-mode-vi V send-keys -X select-line;
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle;
bind-key -T copy-mode-vi y send-keys -X copy-pipe "xclip -in -selection clipboard"

#resize panes (disabled)
#bind k resize-pane -U 10
#bind j resize-pane -D 10
bind [ resize-pane -L 10
bind ] resize-pane -R 10

# map keys to switch panes (vim-style with prefix)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# cycle through panes, staying zoomed if already zoomed
bind -r o if-shell '[ "#{window_zoomed_flag}" = "1" ]' 'select-pane -t :.+ ; resize-pane -Z' 'select-pane -t :.+'
bind -r O if-shell '[ "#{window_zoomed_flag}" = "1" ]' 'select-pane -t :.- ; resize-pane -Z' 'select-pane -t :.-'

# resize pane with ctrl-z
bind -n F1 resize-pane -Z

# equalize pane sizes (like vim ctrl-w =)
bind = select-layout even-horizontal

bind-key -n F2 previous-window
bind-key -n F3 next-window

# move current window left/right (like vim tab movement)
# bind-key '[' swap-window -t:-1\; select-window -t:-1
# bind-key ']' swap-window -t:+1\; select-window -t:+1

# sort sessions by most recently used
bind s choose-tree -sO time

# detach client
#bind d detach
#bind : command-prompt

# set default shell to zsh
set-option -g default-shell /bin/zsh
set -g default-terminal "tmux-256color"

# Ctrl-Left/Right cycles thru panes (no prefix)
# bind-key "C-Left"  select-pane -t :-
# bind-key "C-Right" select-pane -t :+

# set -g @plugin 'tmux-plugins/tmux-copycat'
# set -g @plugin 'tmux-plugins/tmux-yank'
# set -g @yank_selection 'primary'

# source "${HOME}/.tmux-themes/basic.tmuxtheme"



# status bar
set -g status-style "bg=#1e1e2e,fg=#cdd6f4"
set -g status-left " #S  "
set -g status-left-length 30
set -g status-right " %H:%M  %d-%b-%y "
set -g status-right-length 50
set -g status-interval 60
set -g status-justify left

set -g window-status-format " #I:#W "
set -g window-status-current-format " #I:#W "
set -g window-status-current-style "bg=#89b4fa,fg=#1e1e2e,bold"
set -g window-status-style "fg=#6c7086"

set -g pane-border-style "fg=#313244"
set -g pane-active-border-style "fg=#89b4fa"

set -g message-style "bg=#313244,fg=#cdd6f4"

set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @resurrect-strategy-vim 'session'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @continuum-save-interval '120'

# Extended keys support (Shift+Enter, etc.)
set -s extended-keys always
set -as terminal-features 'xterm*:extkeys'

# True color support
set-option -sa terminal-overrides ",xterm-256color:RGB"
set-option -sa terminal-overrides ",tmux-256color:RGB"

# Treat black background as default/transparent background
# Fixes apps that emit explicit bg=black clashing with non-black terminal themes
set -g window-style "bg=default"
set -g window-active-style "bg=default"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'
